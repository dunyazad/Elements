<Window x:Class="Neon.Controls.NodeEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Neon.Nodes"
        Title="Composite Filter Editor" Height="700" Width="1000"
        Background="#2D2D30" Foreground="#DDD"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        BorderBrush="#444" BorderThickness="1"
        Loaded="Window_Loaded">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="5" GlassFrameThickness="0" CornerRadius="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C42B1C"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A32417"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PinStyle" TargetType="Ellipse">
            <Setter Property="Width" Value="14"/>
            <Setter Property="Height" Value="14"/>
            <Setter Property="Stroke" Value="White"/>
            <Setter Property="StrokeThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <EventSetter Event="MouseLeftButtonDown" Handler="Pin_MouseLeftButtonDown"/>
            <EventSetter Event="MouseLeftButtonUp" Handler="Pin_MouseLeftButtonUp"/>
        </Style>

        <DataTemplate DataType="{x:Type local:ConnectionViewModel}">
            <Path Stroke="#AAA" StrokeThickness="2">
                <Path.Data>
                    <PathGeometry>
                        <PathFigure StartPoint="{Binding P1}">
                            <BezierSegment Point1="{Binding P2}" Point2="{Binding P3}" Point3="{Binding P4}"/>
                        </PathFigure>
                    </PathGeometry>
                </Path.Data>
            </Path>
        </DataTemplate>

        <DataTemplate DataType="{x:Type local:NodeViewModel}">
            <Thumb DragDelta="Node_DragDelta" Tag="{Binding}">
                <Thumb.Template>
                    <ControlTemplate>
                        <Border Background="#3E3E42" CornerRadius="5" BorderBrush="#111" BorderThickness="1" MinWidth="160">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" ShadowDepth="3" Opacity="0.5"/>
                            </Border.Effect>
                            <StackPanel>
                                <Border Background="#FF007ACC" CornerRadius="5,5,0,0" Padding="8,4">
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="Bold"/>
                                </Border>

                                <Grid Margin="5,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ItemsControl ItemsSource="{Binding Inputs}" Grid.Column="0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="-12,4,4,4" Tag="{Binding}">
                                                    <Ellipse Style="{StaticResource PinStyle}" Fill="#4CAF50"/>
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="8,0,0,0" FontSize="11"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <ItemsControl ItemsSource="{Binding Outputs}" Grid.Column="2">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal" Margin="4,4,-12,4" HorizontalAlignment="Right" Tag="{Binding}">
                                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="0,0,8,0" FontSize="11"/>
                                                    <Ellipse Style="{StaticResource PinStyle}" Fill="#FF9800"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Thumb.Template>
            </Thumb>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#1E1E1E" MouseLeftButtonDown="TitleBar_MouseDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Composite Filter Editor" VerticalAlignment="Center" Margin="15,0" Foreground="#EEE" FontSize="13" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Click="CloseButton_Click" Content="X" Style="{StaticResource TitleBarCloseButtonStyle}"/>
        </Grid>

        <Grid Grid.Row="1" MouseMove="Editor_MouseMove" MouseLeftButtonUp="Editor_MouseLeftButtonUp" ClipToBounds="True">
            <Grid.Resources>
                <DrawingBrush x:Key="GridBrush" TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute" Opacity="0.1">
                    <DrawingBrush.Drawing>
                        <GeometryDrawing>
                            <GeometryDrawing.Geometry>
                                <GeometryGroup>
                                    <LineGeometry StartPoint="0,0" EndPoint="0,20"/>
                                    <LineGeometry StartPoint="0,0" EndPoint="20,0"/>
                                </GeometryGroup>
                            </GeometryDrawing.Geometry>
                            <GeometryDrawing.Pen>
                                <Pen Brush="White" Thickness="1"/>
                            </GeometryDrawing.Pen>
                        </GeometryDrawing>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Grid.Resources>

            <Rectangle Fill="{StaticResource GridBrush}" IsHitTestVisible="False"/>

            <ItemsControl ItemsSource="{Binding Connections}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

            <Canvas Visibility="{Binding IsDraggingConnection, Converter={StaticResource BoolToVis}}">
                <Line X1="{Binding PendingConnectionSource.X}" Y1="{Binding PendingConnectionSource.Y}"
                      X2="{Binding PendingConnectionTarget.X}" Y2="{Binding PendingConnectionTarget.Y}"
                      Stroke="White" StrokeThickness="2" StrokeDashArray="4 2"/>
            </Canvas>

            <ItemsControl x:Name="NodesControl" ItemsSource="{Binding Nodes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>